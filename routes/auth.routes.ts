// import { Router } from "express";
// import { signIn, signOut, signUp, refreshToken, verifyEmail, resendVerificationCode, requestPasswordReset, verifyPasswordReset } from "../controllers/auth.controller.js";

// const authRouter = Router();

// // Path: api/v1/auth/sign-up (POST)
// authRouter.post('/sign-up', signUp);

// // Path: api/v1/auth/verify-email (POST)
// authRouter.post('/verify-email', verifyEmail);

// // Path: api/v1/auth/resend-verification (POST)
// authRouter.post('/resend-verification', resendVerificationCode);

// // Path: api/v1/auth/sign-in (POST)
// authRouter.post('/sign-in', signIn);

// // Path: api/v1/auth/request-password-reset (POST)
// authRouter.post('/request-password-reset', requestPasswordReset);

// // Path: api/v1/auth/verify-password-reset (POST)
// authRouter.post('/verify-password-reset', verifyPasswordReset);

// // Path: api/v1/auth/refresh-token (POST)
// authRouter.post('/refresh-token', refreshToken);

// // Path: api/v1/auth/sign-out (POST)
// authRouter.post('/sign-out', signOut);

// export default authRouter;